import React from 'react'

function UserList() {
  return (
    <div>
        UserList
        Esta página mostrará una lista de usuarios, la página solo será accesible por usuarios de recepcion y admin.
        Los usuarios mostrados en esta página pueden ser filtrados por número, por nombre o por CI
    </div>
  )
}

export default UserList